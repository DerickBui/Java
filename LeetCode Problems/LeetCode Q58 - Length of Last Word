class Solution {
    public int lengthOfLastWord(String s) {
        if (s.isEmpty()) {
            return 0;
        }
        
        String word [] = s.split(" ");
        
        if (word.length == 0) {
            return 0;
        }
        return word[word.length - 1].length();   
    }
}
